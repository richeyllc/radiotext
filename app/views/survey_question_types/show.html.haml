.page-header
  = link_to survey_question_types_path, class: 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    Back
  = link_to edit_survey_question_type_path(@survey_question_type), class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-pencil
    Edit
  %h1 Show survey_question_type

%dl.dl-horizontal
  %dt Name:
  %dd= @survey_question_type.name
  %dt Description:
  %dd= @survey_question_type.description
