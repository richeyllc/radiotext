.page-header
  = link_to blast_types_path, class: 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    Back
  = link_to edit_blast_type_path(@blast_type), class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-pencil
    Edit
  %h1 Show blast_type

%dl.dl-horizontal
  %dt Name:
  %dd= @blast_type.name
  %dt Description:
  %dd= @blast_type.description
