.page-header
  = link_to new_phone_number_path, class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-plus
    New Phone number
  %h1 Listing phone_numbers

.table-responsive
  %table.table.table-striped.table-bordered.table-hover
    %thead
      %tr
        %th Number
        %th Description
        %th
        %th
        %th

    %tbody
      - @phone_numbers.each do |phone_number|
        %tr
          %td= phone_number.number
          %td= phone_number.description
          %td= link_to 'Show', phone_number
          %td= link_to 'Edit', edit_phone_number_path(phone_number)
          %td= link_to 'Destroy', phone_number, data: { confirm: 'Are you sure?' }, method: :delete
