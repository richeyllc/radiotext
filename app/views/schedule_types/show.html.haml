.page-header
  = link_to schedule_types_path, class: 'btn btn-default' do
    %span.glyphicon.glyphicon-list-alt
    Back
  = link_to edit_schedule_type_path(@schedule_type), class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-pencil
    Edit
  %h1 Show schedule_type

%dl.dl-horizontal
  %dt Name:
  %dd= @schedule_type.name
  %dt Description:
  %dd= @schedule_type.description
