.page-header
  = link_to new_schedule_type_path, class: 'btn btn-primary' do
    %span.glyphicon.glyphicon-plus
    New Schedule type
  %h1 Listing schedule_types

.table-responsive
  %table.table.table-striped.table-bordered.table-hover
    %thead
      %tr
        %th Name
        %th Description
        %th
        %th
        %th

    %tbody
      - @schedule_types.each do |schedule_type|
        %tr
          %td= schedule_type.name
          %td= schedule_type.description
          %td= link_to 'Show', schedule_type
          %td= link_to 'Edit', edit_schedule_type_path(schedule_type)
          %td= link_to 'Destroy', schedule_type, data: { confirm: 'Are you sure?' }, method: :delete
